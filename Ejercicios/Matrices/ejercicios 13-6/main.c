#include <stdio.h>
#include <stdlib.h>
#define s scanf(
#define p printf(

int sumTraza()

void main(){
    int n,fila,columna;
    p"Ingrese la dimension de la matriz");
    s"%d",%d);
    fflush(stdin);

    int matriz(n,n);
    for (fila=0;fila<n;i++){
        for(columna=0;columna<n;i++){
            p"Ingrese valor de la posicion fila %d, columna %d",fila, columna);
            s"%d",&d);
        }
    }
    sumTraza(matriz,n)
}

